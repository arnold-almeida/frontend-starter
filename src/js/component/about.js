/** @jsx React.DOM */

var React = require('react');

module.exports = React.createClass({
  render: function () {
    return (
      <div>
        <h1>About page</h1>
        <p>This is the about page.</p>
      </div>
    );
  }
});
